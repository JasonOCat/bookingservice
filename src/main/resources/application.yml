spring:
  application:
    name: bookingservice
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: none

inventory:
  service:
    url: http://localhost:8080/api/v1/inventory
server:
  port: 8081
